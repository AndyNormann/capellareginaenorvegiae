---
import Layout from "../../layouts/Layout.astro";
import { Image } from "astro:assets";
import HotspotImage from "../../components/HotspotImage.astro";
import ourLadyImg from "@assets/closeupourlady.jpg";
import varfrueImg from "@assets/varfrue.jpg";
import paintingImg from "@assets/our-lady-painting.jpeg";
import statueImg from "@assets/our-lady-statue.jpeg";
import statueRedImg from "@assets/our-lady-statue-red.jpeg";
import { content, defaultLang } from "../../i18n/content";

export function getStaticPaths() {
  return [
    { params: { lang: "en" } },
  ];
}

const { lang } = Astro.params as { lang: string };
const c = lang in content ? content[lang as keyof typeof content] : content[defaultLang];
const ol = c.ourlady;

const artworkSchema = {
  "@type": "VisualArtwork",
  "name": ol.mainImageAlt,
  "description": ol.intro,
  "creator": {
    "@type": "Person",
    "name": "Ayla Gloria Olava Heimly"
  },
  "artworkType": "Painting",
  "locationCreated": {
    "@type": "Place",
    "name": "Gudbrandsdalen, Norway"
  }
};

---

<Layout title={ol.title} description={ol.metaDescription} pageSchema={artworkSchema}>
  <main id="main-content" class="max-w-6xl mx-auto px-4 sm:px-8 pb-24">

    <!-- Header: the dedication -->
    <section class="pt-16 md:pt-24 text-center">
      <h1 class="font-headline text-4xl md:text-5xl italic text-primary leading-tight mb-6">
        {ol.heading}
      </h1>
      <div class="w-16 h-px bg-secondary/40 mx-auto mb-6"></div>
      <p class="font-body text-lg text-on-surface-variant leading-relaxed max-w-xl mx-auto">
        {ol.intro}
      </p>
    </section>

    <!-- The story, and the image -->
    <section class="max-w-5xl mx-auto">
      {ol.blocks.map((block, i) => (
        <div class="mt-16">
          {block.heading && (
            <div class="flex items-center justify-center gap-3 mb-6">
              <h2 class="font-headline text-3xl italic text-primary leading-snug text-center">
                {block.heading}
              </h2>
            </div>
          )}
          <div class="max-w-2xl mx-auto space-y-4 text-on-surface-variant leading-relaxed text-center">
            {block.paragraphs.map((para) => (
              <p>{para}</p>
            ))}
          </div>

          {block.videoEmbed && (
            <div class="max-w-2xl mx-auto mt-8">
              <div class="relative w-full aspect-video rounded-2xl overflow-hidden shadow-xl">
                <iframe
                  src={`https://www.youtube-nocookie.com/embed/${block.videoEmbed}`}
                  title={block.videoTitle}
                  loading="lazy"
                  class="absolute inset-0 w-full h-full border-0"
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                  allowfullscreen
                ></iframe>
              </div>
            </div>
          )}

          {/* Images sit between the first and second block */}
          {i === 0 && (
            <div class="grid grid-cols-1 md:grid-cols-12 gap-6 md:gap-10 items-start mt-12">
              <figure class="md:col-span-7">
                <HotspotImage
                  src={varfrueImg}
                  alt={ol.mainImageAlt}
                  class="rounded-2xl shadow-xl"
                  hotspots={ol.hotspots}
                />
                <figcaption class="mt-3 font-body text-xs text-on-surface-variant italic text-center">
                  {ol.artistCredit}
                </figcaption>
              </figure>
              <div class="md:col-span-5 md:mt-16">
                <Image
                  src={ourLadyImg}
                  alt={ol.imageAlt}
                  class="w-full aspect-[2/3] object-cover rounded-2xl shadow-xl"
                />
              </div>
            </div>
          )}

          {i === 1 && (
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-10">
              <figure class="overflow-hidden rounded-2xl shadow-xl">
                <Image
                  src={paintingImg}
                  alt="Our Lady of Gudbrandsdalen painting"
                  class="w-full aspect-[3/4] object-cover max-h-[500px]"
                  loading="lazy"
                />
                <figcaption class="py-3 px-4 text-center text-sm text-on-surface/70 font-body bg-surface-container-low">
                  Himmelkroning – Maleri fra 1712 i Lom Stavkyrkje
                </figcaption>
              </figure>
              <figure class="overflow-hidden rounded-2xl shadow-xl">
                <Image
                  src={statueImg}
                  alt="Our Lady of Gudbrandsdalen statue"
                  class="w-full aspect-[3/4] object-cover max-h-[500px]"
                  loading="lazy"
                />
                <figcaption class="py-3 px-4 text-center text-sm text-on-surface/70 font-body bg-surface-container-low">
                  Statue av Jomfru Maria fra 1250-1300-tallet, tidligere i Svendsgardskirka, Dovre.
                </figcaption>
              </figure>
            </div>
            <div class="mt-6">
              <figure class="overflow-hidden rounded-2xl shadow-xl max-w-md mx-auto">
                <Image
                  src={statueRedImg}
                  alt="Our Lady of Gudbrandsdalen statue in red"
                  class="w-full aspect-[3/4] object-cover max-h-[500px]"
                  loading="lazy"
                />
                <figcaption class="py-3 px-4 text-center text-sm text-on-surface/70 font-body bg-surface-container-low">
                  Follebumadonnaen - Statue av Jomfru Maria fra 1400-tallet, tidligere i Follebu kirke, Gausdal.
                </figcaption>
              </figure>
            </div>
          )}
        </div>
      ))}
    </section>

    <div class="text-center mt-16">
      <div class="flex flex-wrap justify-center gap-4 mb-8">
        <a
          href={`/${lang}/support`}
          class="inline-flex items-center gap-2 min-h-[44px] px-8 py-2.5 bg-primary text-on-primary rounded-sm font-label text-sm hover:bg-primary/90 transition-colors shadow-md tracking-wide"
        >
          <span class="material-symbols-outlined text-base" aria-hidden="true">favorite</span>
          {ol.supportButton}
        </a>
        <a
          href={`/${lang}/story`}
          class="inline-flex items-center gap-2 min-h-[44px] px-8 py-2.5 border border-primary/40 text-primary rounded-sm font-label text-sm hover:bg-primary/5 transition-colors tracking-wide"
        >
          {ol.storyLink}
          <span class="material-symbols-outlined text-base" aria-hidden="true">arrow_forward</span>
        </a>
      </div>
      <a
        href={`/${lang}`}
        class="inline-flex items-center gap-2 min-h-[44px] px-8 font-label text-sm text-primary hover:text-primary/70 transition-colors tracking-wide"
      >
        <span class="material-symbols-outlined text-base" aria-hidden="true">arrow_back</span>
        {ol.backText}
      </a>
    </div>

  </main>
</Layout>
